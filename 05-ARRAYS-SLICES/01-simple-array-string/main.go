////specify the number of elements it holds in square brackets ([]), followed by the type of elements the array holds.

package main

import "fmt"

func main() {
	var a [2]string
	a[0] = "Hello"
	a[1] = "World"
	fmt.Println(a[0], a[1])
	fmt.Println(a)

}
